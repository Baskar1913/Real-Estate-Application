import { Mail, MapPin, Phone, Send } from "lucide-react";
import { useEffect, useState, type SyntheticEvent } from "react";
import Swal from "sweetalert2";
import api from "../../services/api";
import type { ContactInfoRecord } from "../../types";
export default function ContactPage(){const[form,setForm]=useState({name:"",email:"",subject:"",message:""});const[contact,setContact]=useState<ContactInfoRecord|null>(null);useEffect(()=>{void api.get<ContactInfoRecord[]>("/contact-info/").then(r=>setContact(r.data[0]||null));},[]);const submit=(e:SyntheticEvent<HTMLFormElement>)=>{e.preventDefault();void Swal.fire("Enquiry received","Our property advisor will contact you shortly.","success");setForm({name:"",email:"",subject:"",message:""});};return <><section className="page-banner contact-page-banner"><div className="container"><span className="eyebrow">Let's connect</span><h1>Contact Us</h1><p>We are ready to help you find your dream property.</p></div></section><section className="section container contact-grid"><article><span className="eyebrow">Get in touch</span><h2>Talk to our property advisors</h2><div className="contact-row"><MapPin/><span><strong>Address</strong>{contact?.address||"Office address coming soon"}</span></div><div className="contact-row"><Phone/><span><strong>Phone</strong>{contact?.phone||"Contact number coming soon"}</span></div><div className="contact-row"><Mail/><span><strong>Email</strong>{contact?.email||"Email coming soon"}</span></div>{contact?.map_url&&<div className="contact-row"><MapPin/><span><strong>Location</strong><a href={contact.map_url} target="_blank" rel="noreferrer">View location</a></span></div>}</article><form className="contact-form" onSubmit={submit}><div className="form-two"><label>Your name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label></div><label>Subject<input value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} required/></label><label>Message<textarea rows={6} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} required/></label><button className="gold-button"><Send/>Send message</button></form></section></>}
